@using AleaSim.Client.Services
@using AleaSim.Shared.Models
@inject NavigationManager Navigation
@inject AuthenticationStateProvider AuthStateProvider
@implements IDisposable

<AuthorizeView>
    <Authorized>
        @if (!IsChatPage)
        {
            <div class="floating-chat-root">
                @if (isOpen)
                {
                    <div class="chat-window-popup animate-pop-in">
                        <div class="chat-header-bar pa-3 d-flex align-center justify-space-between">
                            <div class="d-flex align-center gap-2">
                                <div class="online-indicator"></div>
                                <MudText Typo="Typo.subtitle2" Class="fw-black text-white">ALEA CHAT</MudText>
                            </div>
                            <div class="d-flex align-center">
                                <MudIconButton Icon="@Icons.Material.Filled.OpenInFull" Size="Size.Small" Class="text-white opacity-70" OnClick="OpenFullChat" />
                                <MudIconButton Icon="@Icons.Material.Filled.Close" Size="Size.Small" Class="text-white opacity-70" OnClick="ToggleChat" />
                            </div>
                        </div>
                        <div class="chat-body-viewport">
                            <ChatWidget />
                        </div>
                    </div>
                }

                <button class='@($"chat-trigger-btn shadow-2xl {(isOpen ? "active" : "")}")' @onclick="ToggleChat">
                    @if (isOpen) { <MudIcon Icon="@Icons.Material.Filled.KeyboardArrowDown" Style="font-size: 2rem;" /> }
                    else { <MudIcon Icon="@Icons.Material.Filled.ChatBubble" Style="font-size: 1.8rem;" /> }
                </button>
            </div>
        }
    </Authorized>
</AuthorizeView>

<style>
    .floating-chat-root {
        position: fixed;
        bottom: 30px;
        right: 30px;
        z-index: 2000;
        display: flex;
        flex-direction: column;
        align-items: flex-end;
        gap: 20px;
    }

    .chat-trigger-btn {
        width: 64px;
        height: 64px;
        border-radius: 50%;
        background: var(--mud-palette-primary);
        color: white;
        border: none;
        display: flex;
        align-items: center;
        justify-content: center;
        cursor: pointer;
        transition: all 0.4s cubic-bezier(0.34, 1.56, 0.64, 1);
        position: relative;
        box-shadow: 0 8px 25px rgba(124, 58, 237, 0.4);
    }
    
    .chat-trigger-btn:hover { transform: scale(1.1) rotate(5deg); box-shadow: 0 0 25px var(--mud-palette-primary); }
    .chat-trigger-btn.active { transform: scale(0.9); background: var(--mud-palette-surface); color: var(--mud-palette-text-primary); border: 1px solid var(--mud-palette-divider); }

    .chat-window-popup {
        width: 380px;
        height: 550px;
        background: var(--mud-palette-background-grey);
        border: 1px solid var(--mud-palette-divider);
        border-radius: 24px;
        box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.5);
        display: flex;
        flex-direction: column;
        overflow: hidden;
        backdrop-filter: blur(20px);
    }

    .chat-body-viewport {
        flex-grow: 1;
        display: flex;
        flex-direction: column;
        overflow: hidden;
    }

    .animate-pop-in {
        animation: chatPop 0.5s cubic-bezier(0.34, 1.56, 0.64, 1) forwards;
        transform-origin: bottom right;
    }

    @@media (max-width: 600px) {
        .floating-chat-root {
            left: 16px;
            right: 16px;
            bottom: 16px;
            align-items: flex-end;
        }
        .chat-window-popup {
            width: 100%;
            max-width: none;
            height: 500px;
        }
        .animate-pop-in {
            transform-origin: bottom right;
        }
    }

    @@keyframes chatPop {
        from { opacity: 0; transform: scale(0.8) translateY(40px); }
        to { opacity: 1; transform: scale(1) translateY(0); }
    }
</style>

@code {
    private bool isOpen = false;
    private bool IsChatPage => Navigation.Uri.Contains("/chat");

    protected override void OnInitialized() {
        Navigation.LocationChanged += (s, e) => StateHasChanged();
    }

    private void ToggleChat() => isOpen = !isOpen;

    private void OpenFullChat() {
        isOpen = false;
        Navigation.NavigateTo("/chat");
    }

    public void Dispose() { }
}
